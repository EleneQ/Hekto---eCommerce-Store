import styled from "styled-components";

const HeadingBarStyled = styled.section`
  background-color: ${({ theme }) => theme.colors.purple12};
  color: white;
  padding-block: 0.6rem;
  text-align: center;
`;

export default HeadingBarStyled;
